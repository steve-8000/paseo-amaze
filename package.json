{
  "name": "paseo-amaze",
  "version": "0.1.0",
  "private": false,
  "description": "Minimal Paseo provider setup for connecting the local amaze CLI.",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup-amaze-provider.mjs",
    "verify": "node scripts/setup-amaze-provider.mjs --verify-only"
  },
  "engines": {
    "node": ">=20"
  }
}
